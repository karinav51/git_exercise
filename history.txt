# Create the project -mkdir or vscode folder creation

# Initialize the repository -git init

# Track my files -git status

# Add your files -git add {file}

# Commit those files -git commit -m "message"

# Add the remote orgin -git remote add orgin {url}

# Push the changes -git push - u orgin master 

# Track my files

# Add files 

# Commit those files 

# Push the changes